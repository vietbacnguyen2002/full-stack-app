spring:
  application:
    nam: server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:customer_app}?createDatabaseIfNotExist=true
    username: ${DB_USER:admin}
    password: ${DB_PASS:091979818Vip}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

